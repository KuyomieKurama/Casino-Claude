node_modules/
.next/
out/
coverage/
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Verbindungsdaten und Geheimnisse (siehe lib/env.ts, .env.example) — nie einchecken.
.env*
!.env.example

# Ausgabeverzeichnis von scripts/backup-db.sh (Standardziel) — enthält Datenbankinhalte,
# gehört nicht ins Repository.
backups/
